export var base64Regexp = /data:([a-zA-Z]*\/[a-zA-Z]*);base64,([^\"]*)/i;
/**
 * Check if file is blob
 * @param input
 */
export var isFileBlob = function (input) { return input.toString() === '[object Blob]'; };
/**
 * Check if file is instance of named interface
 *
 * @param input
 */
export var isFileNamed = function (input) { return input && input['file'] && input['name']; };
export * from './file_tools.node';
